# 所有宿主机端口可通过环境变量覆盖，避免端口冲突。
# 示例：BACKEND_PORT=18000 FRONTEND_PORT=13000 docker compose up --build -d
# 或在 .env 文件中设置。

services:
  # ── 基础设施 ──

  postgres:
    image: postgres:14
    container_name: swaneval-postgres
    environment:
      POSTGRES_USER: swaneval
      POSTGRES_PASSWORD: swaneval
      POSTGRES_DB: swaneval
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U swaneval"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: swaneval-redis
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── 应用服务 ──

  backend:
    container_name: swaneval-backend
    image: registry.ltkk.run/swaneval/backend:latest
    volumes:
      - swaneval_data:/data
    environment:
      DATABASE_URL: "postgresql+asyncpg://swaneval:swaneval@postgres:5432/swaneval"
      DATABASE_URL_SYNC: "postgresql://swaneval:swaneval@postgres:5432/swaneval"
      REDIS_URL: "redis://redis:6379/0"
      CORS_ORIGINS: '["*"]'
      STORAGE_BACKEND: local
      STORAGE_ROOT: /data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    image: registry.ltkk.run/swaneval/frontend:latest
    container_name: swaneval-frontend
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    environment:
      API_ENDPOINT: http://backend:8000/
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  postgres_data:
  swaneval_data:
  ollama_data:
  minio_data:
